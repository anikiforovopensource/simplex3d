<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<project name="Simplex3dBufferTest" default="release-all" basedir=".">
    <description>Builds, tests, and runs the project Simplex3dBufferTest.</description>
    <import file="nbproject/build-impl.xml"/>
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-jar:                  called before JAR building
      -post-jar:                 called after JAR building
      -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are: 

      -init-macrodef-javac:     defines macro for javac compilation
      -init-macrodef-junit:     defines macro for junit execution
      -init-macrodef-debug:     defines macro for class debugging
      -init-macrodef-java:      defines macro for class execution
      -do-jar-with-manifest:    JAR building (if you are using a manifest)
      -do-jar-without-manifest: JAR building (if you are not using a manifest)
      run:                      execution of project 
      -javadoc-build:           Javadoc generation
      test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

        <target name="run" depends="Simplex3dBufferTest-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that the overridden target depends on the jar target and not only on 
    the compile target as the regular run target does. Again, for a list of available 
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file. 

    -->
    <target name="-init-macrodef-scalac">
        <macrodef name="scalac" uri="http://www.netbeans.org/ns/scala-project/1">
            <attribute default="${src.dir}" name="srcdir"/>
            <attribute default="${build.classes.dir}" name="destdir"/>
            <attribute default="${javac.classpath}" name="classpath"/>
            <attribute default="${extdirs}" name="extdirs"/>
            <attribute default="${includes}" name="includes"/>
            <attribute default="${excludes}" name="excludes"/>
            <attribute default="${scalac.compilerargs}" name="addparams"/>
            <attribute default="" name="sourcepath"/>
            <element name="customize" optional="true"/>
            <sequential>
                <scalac addparams="-make:transitive -dependencyfile ${basedir}/${build.dir}/.scala_dependencies @{addparams}" deprecation="${scalac.deprecation}" destdir="@{destdir}" encoding="${source.encoding}" excludes="@{excludes}" extdirs="@{extdirs}" force="yes" fork="true" jvmargs="-Xss2048k -Xmx1200m" includes="@{includes}" sourcepath="@{sourcepath}" srcdir="@{srcdir}" target="jvm-${javac.target}" unchecked="${scalac.unchecked}">
                    <classpath>
                        <path>
                            <pathelement path="@{classpath}"/>
                            <fileset dir="${scala.lib}">
                                <include name="**/*.jar"/>
                            </fileset>
                            <pathelement location="${build.classes.dir}"/>
                        </path>
                    </classpath>
                    <customize/>
                </scalac>
            </sequential>
        </macrodef>
        <macrodef name="force-recompile" uri="http://www.netbeans.org/ns/scala-project/1">
            <attribute default="${build.classes.dir}" name="destdir"/>
            <sequential>
                <fail unless="javac.includes">Must set javac.includes</fail>
                <pathconvert pathsep="," property="javac.includes.binary">
                    <path>
                        <filelist dir="@{destdir}" files="${javac.includes}"/>
                    </path>
                    <globmapper from="*.scala" to="*.class"/>
                </pathconvert>
                <delete>
                    <files includes="${javac.includes.binary}"/>
                </delete>
            </sequential>
        </macrodef>
    </target>

    <property name="release.dir" value="release"/>
    <property name="release.name" value="simplex3d-buffer-test"/>
    <property name="release.bin" value="${release.dir}/Simplex3dBufferTest_bin.zip"/>
    <property name="release.src" value="${release.dir}/Simplex3dBufferTest_src.zip"/>
    <property name="root.pkg" value=""/>

    <target name="release-all" depends="release-clean, release-src, release-bin" />

    <target name="release-clean" >
        <delete dir="${release.dir}" />
    </target>

    <target name="release-bin" depends="init, clean, compile, jar" >
        <delete file="${release.bin}" />
        <mkdir dir="${release.dir}" />
        <mkdir dir="${release.dir}/jars" />

        <jar compress="${jar.compress}" manifest="${basedir}/manifest.mf"
        jarfile="${release.dir}/jars/${release.name}.jar">
            <fileset dir="${build.classes.dir}" includes="test/** visual/**"/>
        </jar>

        <zip destfile="${release.bin}">
            <fileset dir="${basedir}" includes="lib/**"/>
            <fileset dir="${release.dir}/jars" includes="*.jar"/>
            <fileset dir="${basedir}" includes="
                Readme.txt
                COPYING
                simplex3d-buffer-test.license" />
        </zip>
    </target>

    <target name="release-src" depends="init" >
        <delete file="${release.src}" />
        <mkdir dir="${release.dir}" />

        <zip destfile="${release.src}">
            <fileset dir="${basedir}" includes="lib/**"/>
            <fileset dir="${basedir}" includes="${src.dir}/**" excludes=".svn"/>
            <fileset dir="${basedir}" includes="nbproject/**" excludes=".svn, */private/**"/>
            <fileset dir="${basedir}" includes="
                Readme.txt
                COPYING
                simplex3d-buffer-test.license
                manifest.mf
                build.xml" />
        </zip>
    </target>

</project>
